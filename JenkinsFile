pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                // Replace with your Git repository URL and branch
                checkout([
                    $class: 'GitSCM',
                    branches: [[name: '*/1.20.x']],
                    userRemoteConfigs: [[url: 'https://github.com/Kirderf/compactxpbottles.git']]
                ])
            }
        }
        stage('Hello') {
            steps {
                echo 'Hello, Jenkins!'
            }
        }
        stage('Environment Check') {
            steps {
                script {
                    echo "Running environment check..."
                    sh 'echo "Current directory: $(pwd)"'
                    sh 'echo "Listing files:"'
                    sh 'ls -l'
                }
            }
        }
    }
    post {
        always {
            cleanWs() // Clean the workspace after the build
        }
    }
}
